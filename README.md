# ScoreBoard

## Run released builds
### Interactive Mode 
    ./scoreBoard_<version>

### Demo Mode 
    ./scoreBoard_<version> demo

If not executing run ```chmod +x ./scoreBoard_<version>```

## Build locally
    go build -o scoreboard
    
    OR
    
    go run *.go